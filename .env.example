# API port (default 3210 — avoids conflicts with common dev tools on 3001/3002)
AST_API_PORT=3210
AST_API_HOST=127.0.0.1

# Set to 0 to skip auto-seeding tune jobs on API startup
# AST_AUTO_SEED=0

# Optional: override API URL when not using Vite proxy
# VITE_API_URL=http://localhost:3210
